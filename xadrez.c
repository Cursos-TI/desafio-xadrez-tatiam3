#include <stdio.h>

char xadrez {0} {0} = {
{`T´, `C´, `B´, `Q´, `R´, `B´, `C´, `T´ },
{`P´, `P´, `P´, `P´, `P´, `P´, `P´, `P´ },
{` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´ },
{` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´ },
{` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´ },
{` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´, ` ´ },
{`p´, `p´, `p´, `p´, `p´, `p´, `p´, `p´ },
{`t´, `c´, `b´, `q´, `r´, `b´, `c´, `t´ },
},

int pintartelatexto () {
    system("CLS")
int lin, col;
    for (lin=0; <8; lin++) {
        for (col=0; COL<8; col++} {
printf("\xB2"); 
}
    printf("\n");
}
}
int main()
{
    pintaTelaTexto();
getch();
return0;
}
